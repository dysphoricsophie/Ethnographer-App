# Ethnographer-App

Client-side phenotype distribution editor.

## Versioning
- Application/storage schema version: `v5` (see `STATE_KEY` / `APP_VERSION` in `config.js`).
- Exported profile payload `version` tracks the same app version for consistency.

## Development
- Run tests: `npm test`
